/** @type {import('tailwindcss').Config} */
const config = {
  development: true,
  mode: 'jit',
  darkMode: 'class',
  content: ['./src/**/*.{html,js,svelte,ts}'],
  theme: {
    extend: {},
  },
  plugins: [],
}

export default config
